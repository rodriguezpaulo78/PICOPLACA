<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">


    <item
        android:id="@+id/listar" android:icon="@android:drawable/ic_menu_view"
        app:showAsAction="never" android:title="@string/listar"/>
    <item
        android:id="@+id/copia" android:icon="@android:drawable/ic_dialog_info"
        app:showAsAction="never" android:title="@string/copia">
    </item>

    <!--
    <item
        android:id="@+id/cargar" android:icon="@android:drawable/ic_menu_save"
        app:showAsAction="never" android:title="@string/cargar"/>
    -->
</menu>
